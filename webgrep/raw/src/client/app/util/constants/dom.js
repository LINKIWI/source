// Unique DOM node IDs assigned to various elements.
export const NODE_ID = {
  // Input field for search queries.
  SEARCH_QUERY_FIELD: 'search-query-field',
  // Input field for file path filter queries.
  SEARCH_FILE_PATH_FIELD: 'search-file-path-field',
  // Input field for filtering repositories in the repository selection flyout.
  SEARCH_REPOSITORY_FILTER_FIELD: 'search-repository-filter-field',
};

// All unique DOM node IDs associated with input fields.
export const INPUT_FIELD_IDS = [
  NODE_ID.SEARCH_QUERY_FIELD,
  NODE_ID.SEARCH_FILE_PATH_FIELD,
  NODE_ID.SEARCH_REPOSITORY_FILTER_FIELD,
];

export default undefined;
