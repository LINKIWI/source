// API error codes for describing application-level failure states.
export const ERROR_CODES = {
  // Early termination of the server-side search due to timeout.
  SEARCH_TIMEOUT: 1,
};

export default undefined;
